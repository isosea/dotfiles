{{ if eq .chezmoi.os "darwin" -}}
#!/bin/bash

set -euo pipefail

# Re-run this script whenever the mise tool configuration changes.
# config checksum: {{ include "dot_config/mise/conf.d/tools.toml" | sha256sum }}
mise install --yes
{{ end -}}
